@model PackBuddy.Models.ViewModels.GearTripViewModels.AddGearTripViewModel

@{
    ViewData["Title"] = "Create";
}


<h4 style="text-align:center;">Start Packing!</h4>
<hr />
<form asp-action="Create">
    <div class="gearTrip_GearCardList">
        @for (var i = 0; i < Model.AddedGears.Count; i++)
        {
        <div class="gearTripCreateCard card border-primary mb-3" style="max-width: 9rem;">
            <div class="card-header">@Html.DisplayFor(modelItem => Model.AddedGears[i].Gear.GearType.Label)</div>
            <div class="card-body">
                <h6 class="card-title">@Html.DisplayFor(modelItem => Model.AddedGears[i].Gear.Name)</h6>
                @if (Model.AddedGears[i].Gear.ImagePath != null)
                {
                    <img style=" max-height: 66px; max-width: 6rem" src="~/images/@Model.AddedGears[i].Gear.ImagePath" alt="gearItem picture" />
                }
                else
                {
                    <img style="width: 6rem" src="~/images/@Model.AddedGears[i].Gear.GearType.ImagePath" alt="gearType picture" />
                }
               
            </div>
            <div class="form-group">
                <input type="hidden" asp-for="@Model.Trip.Id" />
                <input type="hidden" asp-for="@Model.AddedGears[i].Gear.Id" />
                <label asp-for="@Model.AddedGears[i].SelectedGear">Pack it?</label><br>
                <input asp-for="@Model.AddedGears[i].SelectedGear" type="checkbox" class="form-control" value="true">
            </div>
        </div>
        }
    </div>
    <div style="text-align:center" class="form-group">
        <input type="submit" value="Pack" class="btn btn-lg btn-primary" />
    </div>
</form>
<div>
    <a asp-controller="Trips" asp-action="Index">Back to Trip List</a>
</div>

